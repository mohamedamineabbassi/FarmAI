package com.farm.backend.config;

// CORS is now fully handled by SecurityConfig.corsConfigurationSource()
// This class is intentionally left empty to avoid duplicate CORS filter conflicts.
// See SecurityConfig.java → corsConfigurationSource() bean.
